---
type: competitor
category: direct
url:
platform:
pricing:
status: active
confidence: assumed
created: <% tp.date.now("YYYY-MM-DD") %>
updated: <% tp.date.now("YYYY-MM-DD") %>
---

# {{title}}

> **category** values: `direct` (same user, same problem, same shape) ·
> `adjacent` (same user, different problem, or vice versa) ·
> `substitute` (what the user currently does instead — spreadsheet,
> notebook, nothing).
>
> **confidence** values: `verified` (I have used it or read primary
> sources) · `cited` (from a reliable secondary source in the vault) ·
> `assumed` (plausible, unchecked) · `guessed` (priors only).

**URL:**
**Platform:**
**Pricing:**
**Status:**

## What They Do

## Strengths

## Weaknesses

## What To Learn From Them

## Mentioned In

```dataview
LIST FROM [[]]
WHERE file.name != this.file.name
```
